{
	"id": "rust",
	"version": "0.1.0",
	"name": "Rust",
	"description": "Template development container for Rust, with Kaylen's dotfiles.",
	"options": {
		"imageVariant": {
			"type": "string",
			"description": "Debian OS version (use bookworm, or bullseye on local arm64/Apple Silicon):",
			"proposals": ["bookworm", "bullseye"],
			"default": "bullseye"
		}
	}
}
